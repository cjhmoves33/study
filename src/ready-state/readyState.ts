document.addEventListener("readystatechange", () => {
  console.log("dom ready state1 -> ", document.readyState);
  console.log("dom ready state1 -> ", document.readyState);
  console.log("dom ready state1 -> ", document.readyState);
  console.log("dom ready state1 -> ", document.readyState);
  console.log("dom ready state1 -> ", document.readyState);
  console.log("dom ready state1 -> ", document.readyState);
  console.log("dom ready state1 -> ", document.readyState);
  console.log("dom ready state1 -> ", document.readyState);
});
